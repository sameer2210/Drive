<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Register | DriveLink</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body
  class="min-h-screen bg-gradient-to-br from-[#0f0f0f] via-[#4d4d4d] to-[#111827] text-white flex items-center justify-center px-4 py-12">
  <% if(error){ %>
    <div class="flex flex-col justify-between items-center gap-8">
      <h1 class="text-4xl text-red-500 font-bold">
        <%=error%>
      </h1>
      <a href="/users/"
        class="text-sm px-5 py-2 bg-red-500 hover:bg-red-600 text-white font-semibold rounded-full shadow transition">
        register
      </a>
      <a href="/users/login"
        class="text-sm px-5 py-2 bg-red-500 hover:bg-red-600 text-white font-semibold rounded-full shadow transition">
        Login
      </a>
    </div>
  <%}else{ %>

      <!-- Navbar -->
      <%- include("./nav") %>

        <!-- Register Box -->
        <div
          class="w-4/2 mt-10 bg-white/5 backdrop-blur-xl border border-white/10 shadow-2xl rounded-2xl px-6 py-6 transition-all duration-300 ring-1 ring-white/10 hover:ring-indigo-500">
          <!-- Heading -->
          <h1 class="text-4xl font-extrabold text-center">Create Account</h1>
          <p class="text-center text-gray-400 text-sm mt-2">
            Join <span class="text-indigo-400 font-semibold">DriveLink</span> today
          </p>

          <!-- Register Form -->
          <form action="/users/create" method="post" class="space-y-3">
            <!-- Name -->
            <div>
              <label for="userName" class="block text-sm text-gray-300 mb-1">user Name</label>
              <input id="userName" name="userName" type="text" required placeholder="John Doe"
                class="w-full px-4 py-3 rounded-lg bg-gray-900 text-white placeholder-gray-500 border border-gray-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 transition" />
            </div>

            <!-- Email -->
            <div>
              <label for="email" class="block text-sm text-gray-300 mb-1">Email</label>
              <input id="email" name="email" type="email" required placeholder="you@example.com"
                class="w-full px-4 py-3 rounded-lg bg-gray-900 text-white placeholder-gray-500 border border-gray-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 transition" />
            </div>

            <!-- Password -->
            <div>
              <label for="password" class="block text-sm text-gray-300 mb-1">Password</label>
              <input id="password" name="password" type="password" required placeholder="••••••••"
                class="w-full px-4 py-3 rounded-lg bg-gray-900 text-white placeholder-gray-500 border border-gray-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 transition" />
            </div>

            <!-- Button -->
            <button type="submit"
              class="w-1/2 py-3 ml-16 bg-indigo-600 hover:bg-indigo-700 text-white font-bold text-lg rounded-md shadow-md hover:shadow-xl transition">
              Register
            </button>
          </form>

          <!-- Login Link -->
          <div class="text-center">
            <p class="text-sm text-gray-400 mt-4">
              Already have an account?
              <a href="/users/login" class="text-indigo-400 hover:text-white underline">Login</a>
            </p>
          </div>
        </div>
  <% } %>
</body>

</html>