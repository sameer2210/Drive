<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>DriveLink</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link
      href="https://cdn.jsdelivr.net/npm/flowbite@3.1.2/dist/flowbite.min.css"
      rel="stylesheet"
    />
  </head>

  <body class="min-h-screen bg-gradient-to-br from-[#0f0f0f] via-[#1a1a1a] to-[#111827] text-white flex items-center justify-center px-4">
    <!-- Center Box -->
    <main class="w-11/12 md:w-5/6 lg:w-4/5 xl:w-3/4 max-w-3xl bg-white/10 backdrop-blur-lg shadow-2xl rounded-2xl p-8 md:p-12 border border-white/20 animate-fadeIn mx-auto">
      
      <!-- Top Bar -->
      <div class="flex justify-end mb-4">
        <a href="/users/login" class="text-sm px-5 py-2 bg-red-500 hover:bg-red-600 text-white font-semibold rounded-full shadow transition">
          Logout
        </a>
      </div>

      <!-- Heading -->
      <div class="text-center mb-10 animate-slideUp">
        <h1 class="text-4xl md:text-5xl font-extrabold text-white mb-3 tracking-tight">
          Welcome to <span class="text-indigo-500">DriveLink</span>
        </h1>
        <p class="text-lg text-gray-300">Securely upload and manage your files with ease.</p>
      </div>

      <!-- Upload Form -->
      <form action="/upload" method="post" enctype="multipart/form-data" class="space-y-6">
        
        <!-- Drag & Drop -->
        <label for="dropzone-file" class="mx-auto flex flex-col items-center justify-center w-full md:w-4/5 h-64 border-2 border-dashed rounded-xl cursor-pointer bg-white/10 hover:bg-white/20 border-gray-200 hover:border-indigo-500 transition-all duration-300">
          <div class="flex flex-col items-center justify-center p-6">
            <svg class="w-12 h-12 text-indigo-400 mb-4" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" d="M3 15a4 4 0 0 1 4-4h.6A2 2 0 0 1 9 10h6a2 2 0 0 1 1.4.6L20 15m-5 4v-6m0 0L12 14m3-1 3 3" />
            </svg>
            <p class="font-semibold text-white">Click to upload</p>
            <p class="text-sm text-gray-300">or drag & drop — (SVG, PNG, JPG or GIF)</p>
          </div>
          <input id="dropzone-file" type="file" name="file" class="hidden" />
        </label>

        <!-- Action Buttons -->
        <div class="flex flex-col md:flex-row gap-4 justify-between items-center pt-4">
          <button type="submit" class="w-full md:w-auto px-6 py-2 bg-indigo-600 text-white font-bold rounded-full hover:bg-indigo-700 transition shadow-md hover:shadow-xl">
            Upload File
          </button>

          <a href="/show" class="w-full md:w-auto px-6 py-2 bg-white text-indigo-600 font-semibold rounded-full hover:bg-slate-200 transition dark:text-white dark:bg-slate-800 dark:hover:bg-slate-700">
            Show Posts
          </a>
        </div>
      </form>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/flowbite@3.1.2/dist/flowbite.min.js"></script>
  </body>
</html>
